CREATE TABLE CategoryList (
	CategoryID	INTEGER,
	CategoryName	TEXT,
	isSelected	INTEGER AS Boolean NOT NULL DEFAULT ((0))
);

select_all_data:
SELECT * FROM CategoryList;

select_data_by_id:
SELECT * FROM CategoryList WHERE CategoryID = ?;